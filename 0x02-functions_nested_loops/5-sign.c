#include "main.h"
/**
 * entry point of a program that prints the sign of a number
 * Return: 1 and if n is greater than zero print +
 * if n is zero it prints 0
 * if n is less than 0 print -1
 */
int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+');
		return (1);
	}
	else if (n == 0);
}
