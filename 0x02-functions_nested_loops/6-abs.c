#include "main.h"

/**
 * Entry point of a program that computed the avbsolute value of an integer
 * @n: integer to check
 * Return: the absolute value of an integer
 */
int _abs(int)
	n=i;
{
	if (n >= 0)
	{
		return (n);
	}
	else	
		return (-n);
}
