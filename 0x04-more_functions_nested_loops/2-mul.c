#include "main.h"
/**
 * Entry point of a program that multiplies two integers
 * @a: the first input
 * @b: the second input
 * Return: result of the multiplication
 */
int mul(int a, int b)
{
return (a * b);
}
